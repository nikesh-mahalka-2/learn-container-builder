# Build Steps:
steps:
 
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args: ['-c', "docker run -v /workspace:/workspace --entrypoint 'bash' ubuntu -c 'echo ${_DB_HLTH_CHK_MAX_RETRY}; chmod +x /workspace/.cloudbuild/check_health.sh; /workspace/.cloudbuild/check_health.sh'"]
