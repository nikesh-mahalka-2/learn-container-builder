# Build Steps:
steps:
 
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args: ['-c', "docker run -v /workspace:/workspace --entrypoint 'bash' ubuntu -c 'chmod +x /workspace/.cloudbuild/check_health.sh; export _DB_HLTH_CHK_MAX_RETRY; /workspace/.cloudbuild/check_health.sh'"]
